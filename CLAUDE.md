# CLAUDE.md

**Read `AGENTS.md` at the repository root before doing any work here, and follow it.**

`AGENTS.md` is the single source for this repository's working rules — post-merge and version handling, Rust/Cargo build artifacts, the shared network layer, and the market-data architecture. This file exists only to point at it; keep the rules there, not here, so every agent reads the same document.

Everything in `AGENTS.md` applies to Claude Code sessions, including worktree-isolated ones.
